В целом требования не противоречат тому, что я вижу со стороны данных и интерфейса. Одно могу подтвердить
  дополнительно, одно противоречие заметил между двумя документами.

  ---
  Что могу подтвердить из своей практики:

  - R-03 (phase_highlights) — мы уже реализовали это как _get_cell_param_pills() в бэкенде. Пайплайн-разработчик должен
   знать: если он добавит готовое поле phase_highlights в данные, мы уберём вычисление на лету в бэкенде. Стоит
  согласовать финальные имена полей: R-03 использует param_id и value_short, наша реализация сейчас называет их code и
  value.
  - R-04 (агрегаты по инструменту) — данные уже есть в pass2_ru.json, пересчёт пайплайна не нужен. Это можно сделать
  как бэкенд-агрегацию прямо сейчас, не дожидаясь R-01.
  - 3C enforcement: R-01 убирает вкладку, но данные 3C остаются в pass2_ru.json — они нужны для матрицы на странице
  инструментов. Пайплайн-разработчику стоит явно это зафиксировать, чтобы он случайно не дропнул 3C-контент.

  ---
  Конфликт между двумя документами:

  Оба документа описывают поле type у источников, но с разными словарями:

  ┌─────────────┬──────────────┐
  │ Документ 1 (И-1) │ Документ 2 (R-05) │
  ├─────────────┼──────────────┤
  │ legislation      │ regulation        │
  ├─────────────┼──────────────┤
  │ rulebook         │ regulation        │
  ├─────────────┼──────────────┤
  │ government       │ regulation        │
  ├─────────────┼──────────────┤
  │ consultation     │ guidance (?)      │
  ├─────────────┼──────────────┤
  │ research         │ academic          │
  ├─────────────┼──────────────┤
  │ other            │ other             │
  └─────────────┴──────────────┘

  Нужно согласовать один словарь до передачи в пайплайн. Я бы взял словарь из Документа 1 — он точнее для регуляторного
   анализа (разница между законодательным актом и правилами биржи существенна для аналитика).