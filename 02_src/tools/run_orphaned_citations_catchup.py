"""
Orphaned citations filter has been removed from the pipeline.

This script is no longer needed — citations are no longer filtered by content.
All citations from Parallel API basis are preserved as-is.
"""

import sys


def main() -> None:
    print("Orphaned citations filter removed — this script is no longer needed.")
    sys.exit(0)


if __name__ == "__main__":
    main()
